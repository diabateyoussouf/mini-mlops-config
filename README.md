# Youssouf MLOps Pipeline

Structure modulaire MLOps avec gestion de configuration multi-environnement (dev/prd) et synchronisation Databricks.

## Setup local avec uv
```bash
uv venv
source .venv/bin/activate
uv pip install -e .
